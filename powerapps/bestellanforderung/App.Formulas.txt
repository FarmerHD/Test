AppTheme = {
    // Core Brand Identity (60/30/10 Rule)
    DarkBlue: ColorValue("#00508E");         // 60% - Main brand color
    LightBlue: ColorValue("#75C0EC");        // 30% - Supporting brand color
    CyanBlue: ColorValue("#10B0DB");         // 10% - Accent brand color

    // Semantic Colors
    DarkGreen: ColorValue("#0c662a");        // Success states
    DarkYellow: ColorValue("#755b0d");       // Warning states
    LightRed: ColorValue("#dc9999");         // Error states
    DeepBlue: ColorValue("#0f2c66");         // Info states

    // Foundation Colors
    White: ColorValue("#ffffff");
    Black: ColorValue("#000000");

    // Text & Content Hierarchy
    DarkGray: ColorValue("#1f2937");         // Main text
    MidGray: ColorValue("#6b7280");          // Supporting text
    LightGray: ColorValue("#9ca3af");        // Subtle text

    // UI Elements
    PaleGray: ColorValue("#e5e7eb");         // Default borders
    OffWhite: ColorValue("#f3f4f6");         // Subtle borders
    NearWhite: ColorValue("#f9fafb")         // Muted backgrounds
};;

// Wert > 5000 -> Freigabe Geschäftsführung
nfWertgrenze = 5000;;

// Web-Link der App (Details > Web-Link)
nfAppLink = "https://apps.powerapps.com/play/HIER-APP-ID-EINTRAGEN";;
nfLinkPrefix = nfAppLink & If(IsBlank(Find("?"; nfAppLink)); "?"; "&") & "antragId=";;

// Rollen aus Liste Empfaenger
nfIstGF = !IsBlank(LookUp(Empfaenger; Rolle.Value = "GF" && Email = User().Email));;
nfMaWiWerke = Filter(Empfaenger; Rolle.Value = "MaWi" && Email = User().Email);;

// Mail-Verteiler
nfMailGF = Concat(Filter(Empfaenger; Rolle.Value = "GF"); Email; ";");;
nfMailInfo = Concat(Filter(Empfaenger; Rolle.Value = "Q" || Rolle.Value = "Beschaffung"); Email; ";");;
nfMailInfoMitGF = Concat(Filter(Empfaenger; Rolle.Value = "Q" || Rolle.Value = "Beschaffung" || Rolle.Value = "GF"); Email; ";")
